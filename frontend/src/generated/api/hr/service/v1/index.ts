// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// AbsenceType represents a configurable type of absence
export type AbsenceType = {
  id?: string;
  tenantId?: number;
  name?: string;
  description?: string;
  color?: string;
  icon?: string;
  deductsFromAllowance?: boolean;
  requiresApproval?: boolean;
  isActive?: boolean;
  sortOrder?: number;
  metadata: wellKnownStruct | undefined;
  requiresSigning?: boolean;
  signingTemplateId?: string;
  createdAt?: wellKnownTimestamp;
  updatedAt?: wellKnownTimestamp;
  createdBy?: number;
  updatedBy?: number;
};

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type CreateAbsenceTypeRequest = {
  //
  // Behaviors: REQUIRED
  tenantId?: number;
  //
  // Behaviors: REQUIRED
  name?: string;
  description?: string;
  color?: string;
  icon?: string;
  deductsFromAllowance?: boolean;
  requiresApproval?: boolean;
  isActive?: boolean;
  sortOrder?: number;
  metadata: wellKnownStruct | undefined;
  requiresSigning?: boolean;
  signingTemplateId?: string;
};

export type CreateAbsenceTypeResponse = {
  absenceType: AbsenceType | undefined;
};

export type GetAbsenceTypeRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetAbsenceTypeResponse = {
  absenceType: AbsenceType | undefined;
};

export type ListAbsenceTypesRequest = {
  tenantId?: number;
  page?: number;
  pageSize?: number;
  noPaging?: boolean;
  query?: string;
};

export type ListAbsenceTypesResponse = {
  items: AbsenceType[] | undefined;
  total?: number;
};

export type UpdateAbsenceTypeRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  data?: AbsenceType;
  updateMask: wellKnownFieldMask | undefined;
};

// In JSON, a field mask is encoded as a single string where paths are
// separated by a comma. Fields name in each path are converted
// to/from lower-camel naming conventions.
// As an example, consider the following message declarations:
//
//     message Profile {
//       User user = 1;
//       Photo photo = 2;
//     }
//     message User {
//       string display_name = 1;
//       string address = 2;
//     }
//
// In proto a field mask for `Profile` may look as such:
//
//     mask {
//       paths: "user.display_name"
//       paths: "photo"
//     }
//
// In JSON, the same mask is represented as below:
//
//     {
//       mask: "user.displayName,photo"
//     }
type wellKnownFieldMask = string;

export type UpdateAbsenceTypeResponse = {
  absenceType: AbsenceType | undefined;
};

export type DeleteAbsenceTypeRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// HrAbsenceTypeService manages absence types
export interface HrAbsenceTypeService {
  CreateAbsenceType(request: CreateAbsenceTypeRequest): Promise<CreateAbsenceTypeResponse>;
  GetAbsenceType(request: GetAbsenceTypeRequest): Promise<GetAbsenceTypeResponse>;
  ListAbsenceTypes(request: ListAbsenceTypesRequest): Promise<ListAbsenceTypesResponse>;
  UpdateAbsenceType(request: UpdateAbsenceTypeRequest): Promise<UpdateAbsenceTypeResponse>;
  DeleteAbsenceType(request: DeleteAbsenceTypeRequest): Promise<wellKnownEmpty>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createHrAbsenceTypeServiceClient(
  handler: RequestHandler
): HrAbsenceTypeService {
  return {
    CreateAbsenceType(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/absence-types`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrAbsenceTypeService",
        method: "CreateAbsenceType",
      }) as Promise<CreateAbsenceTypeResponse>;
    },
    GetAbsenceType(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/absence-types/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrAbsenceTypeService",
        method: "GetAbsenceType",
      }) as Promise<GetAbsenceTypeResponse>;
    },
    ListAbsenceTypes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/absence-types`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.noPaging) {
        queryParams.push(`noPaging=${encodeURIComponent(request.noPaging.toString())}`)
      }
      if (request.query) {
        queryParams.push(`query=${encodeURIComponent(request.query.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrAbsenceTypeService",
        method: "ListAbsenceTypes",
      }) as Promise<ListAbsenceTypesResponse>;
    },
    UpdateAbsenceType(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/absence-types/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "HrAbsenceTypeService",
        method: "UpdateAbsenceType",
      }) as Promise<UpdateAbsenceTypeResponse>;
    },
    DeleteAbsenceType(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/absence-types/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "HrAbsenceTypeService",
        method: "DeleteAbsenceType",
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// LeaveAllowance represents a user's leave allowance for a specific type and year
export type LeaveAllowance = {
  id?: string;
  tenantId?: number;
  userId?: number;
  absenceTypeId?: string;
  year?: number;
  totalDays?: number;
  usedDays?: number;
  carriedOver?: number;
  notes?: string;
  // Denormalized for display
  absenceTypeName?: string;
  userName?: string;
  createdAt?: wellKnownTimestamp;
  updatedAt?: wellKnownTimestamp;
  createdBy?: number;
  updatedBy?: number;
};

export type CreateAllowanceRequest = {
  //
  // Behaviors: REQUIRED
  tenantId?: number;
  //
  // Behaviors: REQUIRED
  userId?: number;
  //
  // Behaviors: REQUIRED
  absenceTypeId?: string;
  //
  // Behaviors: REQUIRED
  year?: number;
  //
  // Behaviors: REQUIRED
  totalDays?: number;
  carriedOver?: number;
  notes?: string;
  userName?: string;
};

export type CreateAllowanceResponse = {
  allowance: LeaveAllowance | undefined;
};

export type GetAllowanceRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetAllowanceResponse = {
  allowance: LeaveAllowance | undefined;
};

export type ListAllowancesRequest = {
  tenantId?: number;
  page?: number;
  pageSize?: number;
  noPaging?: boolean;
  // Filters
  userId?: number;
  year?: number;
  absenceTypeId?: string;
};

export type ListAllowancesResponse = {
  items: LeaveAllowance[] | undefined;
  total?: number;
};

export type UpdateAllowanceRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  data?: LeaveAllowance;
  updateMask: wellKnownFieldMask | undefined;
};

export type UpdateAllowanceResponse = {
  allowance: LeaveAllowance | undefined;
};

export type DeleteAllowanceRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// BalanceEntry represents balance for a single absence type
export type BalanceEntry = {
  absenceTypeId: string | undefined;
  absenceTypeName: string | undefined;
  color: string | undefined;
  totalDays: number | undefined;
  usedDays: number | undefined;
  carriedOver: number | undefined;
  remainingDays: number | undefined;
};

export type GetUserBalanceRequest = {
  //
  // Behaviors: REQUIRED
  userId: number | undefined;
  year?: number;
};

export type GetUserBalanceResponse = {
  userId: number | undefined;
  year: number | undefined;
  entries: BalanceEntry[] | undefined;
};

// HrAllowanceService manages leave allowances
export interface HrAllowanceService {
  CreateAllowance(request: CreateAllowanceRequest): Promise<CreateAllowanceResponse>;
  GetAllowance(request: GetAllowanceRequest): Promise<GetAllowanceResponse>;
  ListAllowances(request: ListAllowancesRequest): Promise<ListAllowancesResponse>;
  UpdateAllowance(request: UpdateAllowanceRequest): Promise<UpdateAllowanceResponse>;
  DeleteAllowance(request: DeleteAllowanceRequest): Promise<wellKnownEmpty>;
  GetUserBalance(request: GetUserBalanceRequest): Promise<GetUserBalanceResponse>;
}

export function createHrAllowanceServiceClient(
  handler: RequestHandler
): HrAllowanceService {
  return {
    CreateAllowance(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/allowances`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrAllowanceService",
        method: "CreateAllowance",
      }) as Promise<CreateAllowanceResponse>;
    },
    GetAllowance(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/allowances/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrAllowanceService",
        method: "GetAllowance",
      }) as Promise<GetAllowanceResponse>;
    },
    ListAllowances(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/allowances`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.noPaging) {
        queryParams.push(`noPaging=${encodeURIComponent(request.noPaging.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.year) {
        queryParams.push(`year=${encodeURIComponent(request.year.toString())}`)
      }
      if (request.absenceTypeId) {
        queryParams.push(`absenceTypeId=${encodeURIComponent(request.absenceTypeId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrAllowanceService",
        method: "ListAllowances",
      }) as Promise<ListAllowancesResponse>;
    },
    UpdateAllowance(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/allowances/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "HrAllowanceService",
        method: "UpdateAllowance",
      }) as Promise<UpdateAllowanceResponse>;
    },
    DeleteAllowance(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/allowances/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "HrAllowanceService",
        method: "DeleteAllowance",
      }) as Promise<wellKnownEmpty>;
    },
    GetUserBalance(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.userId) {
        throw new Error("missing required field request.user_id");
      }
      const path = `v1/users/${request.userId}/balance`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.year) {
        queryParams.push(`year=${encodeURIComponent(request.year.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrAllowanceService",
        method: "GetUserBalance",
      }) as Promise<GetUserBalanceResponse>;
    },
  };
}
export type HrErrorReason =
  // 400
  | "BAD_REQUEST"
  | "VALIDATION_FAILED"
  | "INVALID_DATE_RANGE"
  | "INSUFFICIENT_ALLOWANCE"
  // 404
  | "NOT_FOUND"
  | "ABSENCE_TYPE_NOT_FOUND"
  | "LEAVE_REQUEST_NOT_FOUND"
  | "ALLOWANCE_NOT_FOUND"
  // 409
  | "ALREADY_EXISTS"
  | "OVERLAP_EXISTS"
  | "ABSENCE_TYPE_IN_USE"
  // 500
  | "INTERNAL_SERVER_ERROR";
// LeaveRequestStatus represents the status of a leave request
export type LeaveRequestStatus =
  | "LEAVE_REQUEST_STATUS_UNSPECIFIED"
  | "LEAVE_REQUEST_STATUS_PENDING"
  | "LEAVE_REQUEST_STATUS_APPROVED"
  | "LEAVE_REQUEST_STATUS_REJECTED"
  | "LEAVE_REQUEST_STATUS_CANCELLED"
  | "LEAVE_REQUEST_STATUS_AWAITING_SIGNING"
  | "LEAVE_REQUEST_STATUS_REVOKED";
// LeaveRequest represents a user's leave/absence request
export type LeaveRequest = {
  id?: string;
  tenantId?: number;
  userId?: number;
  absenceTypeId?: string;
  startDate?: wellKnownTimestamp;
  endDate?: wellKnownTimestamp;
  days?: number;
  status?: LeaveRequestStatus;
  reason?: string;
  reviewNotes?: string;
  reviewedBy?: number;
  reviewedAt?: wellKnownTimestamp;
  notes?: string;
  metadata: wellKnownStruct | undefined;
  // Denormalized fields for display
  userName?: string;
  userEmail?: string;
  absenceTypeName?: string;
  absenceTypeColor?: string;
  reviewerName?: string;
  orgUnitName?: string;
  signingRequestId?: string;
  createdAt?: wellKnownTimestamp;
  updatedAt?: wellKnownTimestamp;
  createdBy?: number;
  updatedBy?: number;
};

export type CreateLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  tenantId?: number;
  //
  // Behaviors: REQUIRED
  userId?: number;
  //
  // Behaviors: REQUIRED
  absenceTypeId?: string;
  //
  // Behaviors: REQUIRED
  startDate?: wellKnownTimestamp;
  //
  // Behaviors: REQUIRED
  endDate?: wellKnownTimestamp;
  days?: number;
  reason?: string;
  notes?: string;
  metadata: wellKnownStruct | undefined;
  userName?: string;
  userEmail?: string;
  orgUnitName?: string;
};

export type CreateLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

export type GetLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

export type ListLeaveRequestsRequest = {
  tenantId?: number;
  page?: number;
  pageSize?: number;
  noPaging?: boolean;
  query?: string;
  // Filters
  userId?: number;
  absenceTypeId?: string;
  status?: LeaveRequestStatus;
  startDate?: string;
  endDate?: string;
};

export type ListLeaveRequestsResponse = {
  items: LeaveRequest[] | undefined;
  total?: number;
};

export type UpdateLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  data?: LeaveRequest;
  updateMask: wellKnownFieldMask | undefined;
};

export type UpdateLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

export type DeleteLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// ApproveLeaveRequestRequest approves a pending leave request
export type ApproveLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewNotes?: string;
  approverEmail?: string;
  approverName?: string;
  requesterEmail?: string;
};

export type ApproveLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

// RejectLeaveRequestRequest rejects a pending leave request
export type RejectLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewNotes?: string;
};

export type RejectLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

// CancelLeaveRequestRequest cancels a leave request
export type CancelLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CancelLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

// RevokeLeaveRequestRequest revokes an approved leave request
export type RevokeLeaveRequestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reason?: string;
};

export type RevokeLeaveRequestResponse = {
  leaveRequest: LeaveRequest | undefined;
};

// CalendarEvent represents a leave request displayed on calendar
export type CalendarEvent = {
  id: string | undefined;
  userId: number | undefined;
  userName: string | undefined;
  absenceTypeId: string | undefined;
  absenceTypeName: string | undefined;
  color: string | undefined;
  startDate: wellKnownTimestamp | undefined;
  endDate: wellKnownTimestamp | undefined;
  days: number | undefined;
  status: LeaveRequestStatus | undefined;
  orgUnitName: string | undefined;
};

// GetSignedDocumentUrlRequest returns a download URL for the signed document
// Only allowed for participants (request owner or reviewer) or admins
export type GetSignedDocumentUrlRequest = {
  //
  // Behaviors: REQUIRED
  leaveRequestId: string | undefined;
};

export type GetSignedDocumentUrlResponse = {
  url: string | undefined;
};

export type GetCalendarEventsRequest = {
  tenantId?: number;
  startDate?: string;
  endDate?: string;
  orgUnitName?: string;
  userId?: number;
};

export type GetCalendarEventsResponse = {
  events: CalendarEvent[] | undefined;
};

// HrLeaveService manages leave requests
export interface HrLeaveService {
  CreateLeaveRequest(request: CreateLeaveRequestRequest): Promise<CreateLeaveRequestResponse>;
  GetLeaveRequest(request: GetLeaveRequestRequest): Promise<GetLeaveRequestResponse>;
  ListLeaveRequests(request: ListLeaveRequestsRequest): Promise<ListLeaveRequestsResponse>;
  UpdateLeaveRequest(request: UpdateLeaveRequestRequest): Promise<UpdateLeaveRequestResponse>;
  DeleteLeaveRequest(request: DeleteLeaveRequestRequest): Promise<wellKnownEmpty>;
  ApproveLeaveRequest(request: ApproveLeaveRequestRequest): Promise<ApproveLeaveRequestResponse>;
  RejectLeaveRequest(request: RejectLeaveRequestRequest): Promise<RejectLeaveRequestResponse>;
  CancelLeaveRequest(request: CancelLeaveRequestRequest): Promise<CancelLeaveRequestResponse>;
  RevokeLeaveRequest(request: RevokeLeaveRequestRequest): Promise<RevokeLeaveRequestResponse>;
  GetCalendarEvents(request: GetCalendarEventsRequest): Promise<GetCalendarEventsResponse>;
  GetSignedDocumentUrl(request: GetSignedDocumentUrlRequest): Promise<GetSignedDocumentUrlResponse>;
}

export function createHrLeaveServiceClient(
  handler: RequestHandler
): HrLeaveService {
  return {
    CreateLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/leave-requests`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrLeaveService",
        method: "CreateLeaveRequest",
      }) as Promise<CreateLeaveRequestResponse>;
    },
    GetLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrLeaveService",
        method: "GetLeaveRequest",
      }) as Promise<GetLeaveRequestResponse>;
    },
    ListLeaveRequests(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/leave-requests`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.noPaging) {
        queryParams.push(`noPaging=${encodeURIComponent(request.noPaging.toString())}`)
      }
      if (request.query) {
        queryParams.push(`query=${encodeURIComponent(request.query.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.absenceTypeId) {
        queryParams.push(`absenceTypeId=${encodeURIComponent(request.absenceTypeId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`)
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrLeaveService",
        method: "ListLeaveRequests",
      }) as Promise<ListLeaveRequestsResponse>;
    },
    UpdateLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "HrLeaveService",
        method: "UpdateLeaveRequest",
      }) as Promise<UpdateLeaveRequestResponse>;
    },
    DeleteLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "HrLeaveService",
        method: "DeleteLeaveRequest",
      }) as Promise<wellKnownEmpty>;
    },
    ApproveLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}/approve`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrLeaveService",
        method: "ApproveLeaveRequest",
      }) as Promise<ApproveLeaveRequestResponse>;
    },
    RejectLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}/reject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrLeaveService",
        method: "RejectLeaveRequest",
      }) as Promise<RejectLeaveRequestResponse>;
    },
    CancelLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}/cancel`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrLeaveService",
        method: "CancelLeaveRequest",
      }) as Promise<CancelLeaveRequestResponse>;
    },
    RevokeLeaveRequest(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/leave-requests/${request.id}/revoke`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "HrLeaveService",
        method: "RevokeLeaveRequest",
      }) as Promise<RevokeLeaveRequestResponse>;
    },
    GetCalendarEvents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/calendar`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`)
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`)
      }
      if (request.orgUnitName) {
        queryParams.push(`orgUnitName=${encodeURIComponent(request.orgUnitName.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrLeaveService",
        method: "GetCalendarEvents",
      }) as Promise<GetCalendarEventsResponse>;
    },
    GetSignedDocumentUrl(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.leaveRequestId) {
        throw new Error("missing required field request.leave_request_id");
      }
      const path = `v1/leave-requests/${request.leaveRequestId}/signed-document`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrLeaveService",
        method: "GetSignedDocumentUrl",
      }) as Promise<GetSignedDocumentUrlResponse>;
    },
  };
}
// HealthCheckRequest for health checks
export type HealthCheckRequest = {
};

export type HealthCheckResponse = {
  status: string | undefined;
  version: string | undefined;
  timestamp: wellKnownTimestamp | undefined;
};

// GetStatsRequest retrieves HR module statistics
export type GetStatsRequest = {
  tenantId?: number;
};

export type GetStatsResponse = {
  totalAbsenceTypes: number | undefined;
  pendingRequests: number | undefined;
  approvedRequests: number | undefined;
  rejectedRequests: number | undefined;
  totalRequests: number | undefined;
};

// HrSystemService provides system-level operations
export interface HrSystemService {
  // Health check
  HealthCheck(request: HealthCheckRequest): Promise<HealthCheckResponse>;
  // Get HR statistics
  GetStats(request: GetStatsRequest): Promise<GetStatsResponse>;
}

export function createHrSystemServiceClient(
  handler: RequestHandler
): HrSystemService {
  return {
    HealthCheck(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/health`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrSystemService",
        method: "HealthCheck",
      }) as Promise<HealthCheckResponse>;
    },
    GetStats(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/stats`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.tenantId) {
        queryParams.push(`tenantId=${encodeURIComponent(request.tenantId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrSystemService",
        method: "GetStats",
      }) as Promise<GetStatsResponse>;
    },
  };
}
// Lightweight user representation for HR module dropdowns
export type HrUser = {
  id: number | undefined;
  username: string | undefined;
  realname: string | undefined;
  email: string | undefined;
  orgUnitNames: string[] | undefined;
  positionNames: string[] | undefined;
  avatar?: string;
};

export type ListHrUsersRequest = {
  noPaging?: boolean;
};

export type ListHrUsersResponse = {
  items: HrUser[] | undefined;
  total: number | undefined;
};

// HrUserService provides user listing for HR module
export interface HrUserService {
  ListUsers(request: ListHrUsersRequest): Promise<ListHrUsersResponse>;
}

export function createHrUserServiceClient(
  handler: RequestHandler
): HrUserService {
  return {
    ListUsers(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/users`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.noPaging) {
        queryParams.push(`noPaging=${encodeURIComponent(request.noPaging.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "HrUserService",
        method: "ListUsers",
      }) as Promise<ListHrUsersResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
